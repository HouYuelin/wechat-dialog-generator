# 更新日志

## 未发布

### 新增

- 定时播放：预览区新增播放条，聊天内容按节奏逐条出现而不是一次全部展示，可选择快 / 正常 / 慢三档速度，方便直接录屏；播放免费，不消耗导出额度。
- 发送消息音效：自己发出消息时也有一声提示音，音色和「收到」明显不同（微信式「咻」），可在播放条和视频导出弹窗里分别开关，默认关闭。
- 视频导出：新增「生成视频」入口，把逐条播放的过程录成视频并下载。导出弹窗内可选画面尺寸（9:16 竖屏 / 1125×2436 满屏 / 跟随屏幕尺寸）、播放节奏、收到与发送两种音效的独立开关，以及提示音音源（内置合成音或上传自己的音频）。
- 收到消息提示音：视频导出时按消息节奏播放微信风格提示音，内置音由 Web Audio 实时合成，不引入第三方音频文件。
- 屏幕尺寸（导出分辨率）：预览弹层内可选 1080×1920 / 1080×2340 / 1125×2436 / 1170×2532 / 1290×2796 / 1440×3120 等常见机型分辨率，也可自定义宽高。导出的图片与视频帧按它输出，预览画面比例同步变化。
- 图片大小：预览弹层内新增一段，可选 小 300 / 标准 420 / 大 540 / 超大 700，也可自定义最长边（160–700px）。此前图片消息固定按最大边 420px 渲染，长图和主图会显得偏小。
- 字体大小：预览弹层内新增一段，可选 紧凑 80% / 偏小 90% / 标准 100% / 偏大 115% / 特大 130%，也可自定义比例（70–140%）。字号、行高、气泡留白、头像与消息间距会一起缩放，所以调小之后一屏能放下更多条消息，而不是只有字变小、框还是原来那么大。
- 素材库：上传过的头像、表情图片、背景图与商品图都会留在这台浏览器里（IndexedDB 的 `media-assets`），下次在任意对话中直接点选复用，也可以重新上传。弹窗内可改名、删除，并按「头像 / 表情图片 / 背景图 / 商品图」四类给出数量与占用体积。
- 批量上传：素材库弹窗支持一次选多张，或把一堆图直接拖进来；「角色头像」面板新增「批量上传头像」，按选择顺序为角色逐个配图，多出的只入库不影响已有角色。
- 背景图进素材库：聊天背景与朋友圈封面上传过的图都会存进素材库（新的「背景图」类目），两处各多了一行「最近用过」的缩略条，点一张直接换上；「上传 / 管理」可以一次传几张、改名、删除。此前背景图只跟着那一个项目 / 草稿走，换项目就要重传。
- 音效库：上传过的自定义提示音会留在这台浏览器里（IndexedDB 的 `sound-assets`），下次在播放条或视频导出弹窗里直接从「音效库」点选，也可以改名、删除；收发两声提示音各选各的，选中的替换对应那一声，没选的仍用内置合成音。此前自定义音效只存在当前页面里，刷新或换对话就丢。
- 收发提示音都可自定义：「我的音频」音源下，「收到消息」和「发送消息」各自有「选择音频 / 更换」与「音效库」入口，可以两条用不同的音频，也可以用同一条；预览播放与导出视频使用同一套音源，听感一致。此前上传的音频只能替换「收到消息」那一声。
- 商品图：素材库新增第四个类目「商品图」，上传、批量上传（多选 / 拖拽）、选取、改名、删除、数量与体积统计都与头像 / 表情图片 / 背景图一致；「添加消息 → 图片」下也多了一行商品图快捷条。它和表情图片一样能发进对话（点一张即设为待发图片、也能换掉某条消息的图），只是单独归类方便查找。此前商品图只能混在表情图片里。
- 昵称记忆：输入过的昵称会留在这台浏览器里，下次在输入框下面那一行「最近用过」里点一下直接填上，不用重打。覆盖聊天标题、朋友圈昵称 / 评论人 / 点赞用户、场景页的收款方 / 发送人 / 昵称 / 群名 / 成员昵称、导入过的角色名、以及头像归档改名时的名字；每个名字右侧的 × 可以把它从历史里删掉。
- 设置记忆（我的偏好）：凡是能调的参数都会留在这台浏览器里（localStorage 的 `wechat-dialog-generator:prefs`）——样式（平台、状态栏、气泡配色、聊天背景、图片大小、字号）、播放与导出（节奏、提示音开关与音源、上次选的音效、屏幕尺寸、视频画面与滚动时长）、以及预览的窗口宽度。刷新页面、新建空白对话、导入新的聊天内容都继续用上次的值，**不再恢复默认**；只有聊天标题跟着内容走。此前音效、屏幕尺寸、节奏、导出参数只活在当前页面里（刷新即回默认），新建空白对话还会把整套样式一起重置。
- 批量页的素材库：批量聊天制作现在也有完整素材库——头像可以从库里选、上传即入库、按选择顺序批量给角色配头像，点图片消息气泡直接从库里换图（表情图片与商品图都能用），聊天背景也能多张管理与「最近用过」。库与聊天页共用同一份，你在哪一页传的图，另一页都看得见；「用过的头像」归档在批量页同样可以一键还给同名角色。
- 微信表情：新增表情面板，正文里直接写 `[呲牙]`、`[强]`、`[玫瑰]` 这类标签（或从面板里点）就会显示成对应表情，导入的聊天记录里带标签也是同样效果。共 136 个标签，按「表情 / 手势 / 物件 / 新表情」分四组，面板顶部是「最近用过」。入口在「添加消息 → 文字」的输入框下方，转账备注、聊天记录文本框、批量页的批量文本框 / 本组聊天记录 / 逐条消息编辑、朋友圈正文也都能用。**存的是标签、显示的是表情**：文本里躺的是几个字节的 `[呲牙]`，渲染时才变成 😁，所以导入导出、项目文件、分享链接都原样往返，也不会把文本撑大（和图片消息只写 `[图片]` 是同一个道理）。
- 批量页的视频导出：批量聊天制作新增「聊天视频」产出，在右侧「预览与导出」里和「聊天图」二选一切换，选哪项记进「我的偏好」，刷新或重新导入都还在。逐组把对话录成视频、**每组录完立即下载**，不打包成一个 ZIP；录制方式（逐条播放 / 长截图滚动）、画面尺寸、节奏、收发提示音与音源，和聊天页共用同一份设置。导出方式与提示音面板沿用聊天页那套，标题换成「批量视频设置」，并会说明「每组使用 1 次导出额度，本批共 N 次」，额度仍按组扣。
- 两侧留白（安全区）：导出新增「两侧留白」——左右各让出一段对话底色的空白。发抖音时 9:16 的视频在 iPhone Pro Max 这类 19.5:9 的屏幕上会被「铺满」播放，左右各被裁掉约 9%（1080 宽的视频就是各 97px），右侧那排按钮与左下角文字也压在画面两侧；图文发出去被裁掉、被压住的同样是左右两头——被裁掉和被压住的就是这两条留白，聊天内容本身不受影响。快捷档 关闭 / 少量 60 / 标准 120 / 多留 200，也可以直接填 0–300 的像素值；默认「标准 120」（抖音安全区推荐值），开箱即用。**图片与视频都支持**：生成图片、长截图、复制、批量聊天图会照这个像素直接加在导出图的左右两头，画面比例一个像素都不动（1125 宽 + 120 → 1365 宽）；视频的画布尺寸是固定的，留白越过画面自然留边（9:16 约 97px）之后会把手机的画面比例换成「画布去掉两侧留白」那一份，所以**只左右留白、上下始终铺满**（多出来的高度落在聊天区，消息一条不动），不会像等比缩小那样上下也空出一截。留白是「至少这么多」，调到自然留边以下不再有效果。单聊页与批量页共用同一份设置，也记进「我的偏好」。
- 预览里的取景框：开着「两侧留白」时，预览区直接变成「导出后的那一帧」——手机按导出比例缩进画布、铺满上下居中，两侧露出来的底色就是成片里的边带，调多少当场变多少，不用再导一遍发出去才知道够不够。留白控件也搬进了预览右上角的尺寸弹层（和屏幕尺寸、图片大小、字号并列），弹层不挡画面，可以边看边调；和导出弹窗里那个是同一个值。取景框按视频画布（如 1080×1920）绘制，下面那行会写「图片与长截图会照同样的像素加在左右两头」；取景框里的手机没有机身外框，只有一条虚线标出内容边界——那个位置常常和边带同色，不标看不出留白有多少。图片工作时不画取景框，但导出会照常带上留白。把留白调成「关闭」预览就回到原来的手机视图。开着取景框时预览会略窄一点（要连边带一起显示），看细节用右上角的专注模式放大即可；「窗口大小」这一档在取景框开着时调的是取景框的宽度。批量页在「聊天视频」模式下同样生效。

### 改进

- 「添加消息」会写回聊天记录文本：在聊天内容里新加的文字 / 图片 / 红包 / 转账 / 语音 / 时间消息，都按同样的记录格式追加到上方文本框末尾，可以在这里继续改写再点「解析并导入」。此前这些消息只活在编辑状态里，加完就再也没法修改；文本框下方也补了一句说明。图片消息只写 `[图片]` 标记，不把本地图片的 data URL 塞进输入框。
- 转账备注里再出现冒号不再被截断：解析只按第一个冒号切分金额与备注，「[转账]88:还你的，备注：带冒号」现在整段都是备注。
- 素材库每类上限提高到 2000 张，并且**再也不会自动删除**：到上限只是不再收录新图（回执会说明几张没进来），已保存的素材只会在你点删除时消失。同一张图重复上传仍只会留一条。
- 用过的头像：给角色配过的头像按角色名长期留一份，换对话、重新导入、清空编辑器都不会再丢；「角色头像」面板下半部分列出全部用过的头像，可以一键还给同名角色，也可以**改名**（行内编辑，Enter 提交 / Escape 取消）或逐条手动删除。重新导入时按名字自动对回去，对不上的名字不会顶掉已有记录。
- 上传入口统一到一条链路：点头像卡片上传、从素材库选、给某条图片消息换图、上传聊天背景与朋友圈封面，走的都是「读图 → 超过最长边 1280px 才压缩 → 入库」，动图与矢量图原样保留。素材库不可用（隐私模式等）时上传照常，只是留不到下次。
- 图片消息气泡点击改为打开素材库（含现传的新图），素材库不可用时退回原来的文件选择。
- 视频结尾那一帧的停留时间从 1.6 秒延长到 3 秒，录屏导出都有更充裕的收尾时间（预览播放与导出一致，改的是同一个常量 `playbackTailMs`）。
- 「满屏 1125×2436」与「跟随屏幕」两档的说明改了口径：它们不再是「绝对铺满」，而是分辨率跟着屏幕 / 长截图走，四周留多少仍由「两侧留白」决定；想回到真正的满屏，把留白调成「关闭」即可。
- 重新导入聊天记录不再丢失已上传的头像：头像与「自己」身份按**名字**复用（每次解析用户 id 都会重排，名字才稳定），导入成功的提示会说明沿用了几个头像。批量导出改用同一份复用规则。
- 内置提示音向微信靠拢：接收音从 1318→988 Hz 的正四度改为 **988→784 Hz 的下行大三度**（更接近微信「叮咚」那声温润的木质感，不再像电子门铃），发送音收紧得更短更干脆。仍然现场合成，不打包任何第三方音频文件；要逐样本一致只能把音源改成「我的音频」用你自己的文件。
- 游客每日免费导出额度从 10 次调整为 100000 次，额度常量收敛为 `GUEST_DAILY_LIMIT`，界面、分享卡片与 SEO 描述统一引用该值。
- 预览区新增「专注模式」，可隐藏编辑区放大预览，便于录屏。
- 预览区新增「聊天窗口大小」调节：自适应 / 小 320px / 标准 375px / 大 430px / 超大 500px，也可手动输入 200–720px。它只影响页面上的显示大小，成片像素改由「屏幕尺寸」决定。
- 播放与视频导出共用同一套时间线，预览看到的速度即导出后的速度。
- 导出日志与产品埋点补充 `video` 导出类型与 `video_exported` 事件。
- 播放进行中会禁用图片导出，避免录屏中途截到不完整的对话。
- 昵称历史是**一份共用的清单**，四个工具里的输入处共用它：按与头像归档相同的规则归一化（忽略大小写与空白），所以「小林」「小林 」只占一格；去重之外按「最近用过」排序，上限 100 个，超出淘汰最久没用的，导入一份几十人的聊天记录也不会把它撑爆。「成员昵称」「点赞用户」这类一格多人的输入，点筹码是**追加**而不是覆盖（同名的自动跳过），也仍然可以直接用逗号 / 顿号一次写多个。
- 素材库弹窗底部的按钮按「为什么打开」细分文案了：设为头像 / 用作背景 / 换这张（换某条消息的图）/ 用这张（放进待添加的图片）/ 使用。
- 表情标签与特殊消息格式互不干扰：`[图片]`、`[红包]`、`[转账]`、`[语音]` 仍按消息类型解析，不会被当成表情；认不出来的方括号（`[待办]`、`[链接](url)`、`[这是一个很长的说明]`）原样留在正文里。红包备注刻意不认标签——微信的红包留言本身不支持表情——转账备注认。
- 消息正文与朋友圈正文的渲染从「拼 HTML 字符串 + 手动转义」改成 React 元素（新增 `EmojiText`）：标签里的尖括号天然是纯文本，调用点不用各自记得转义，少一处漏转义就少一个 XSS 缺口。
- 「加载示例」「批量示例」和豆包 / 批量 Prompt 里都带上了表情标签，照着示例就能看出标签怎么用；批量 Prompt 多了一条「需要表情时直接写微信表情标签，不要输出 emoji 字符或表情图片链接」的规则。
- 「图片」消息的素材快捷条改为按类目自动生成（现在有表情图片与商品图两条），以后再加能发图的类目不用再补一处。
- 自定义音效上传一律入库：播放条上「直接选文件」的那条路以前只作用于当前页面，下次就找不到了，现在和音效库里的上传是同一条链路（同一段音频传两次仍只留一条，并复用原记录的 id，偏好才指得准）。
- 打开已保存的草稿时只换内容，样式继续用你的偏好：**草稿不再携带样式**。旧草稿里存的往往就是产品默认值，照它渲染等于「一开草稿就把我设好的配色、字号冲回默认」。项目快照里仍然照常保存整份设置（同款分享链接要用到它），只是打开草稿时不再拿它当样式来源。
- 套用同款模板链接是唯一的例外：那一处仍然用链接里的样式（这就是「套用」的意思），链接里缺的字段（比如旧链接没有字号）用**当前的偏好**补，而不是产品默认值。
- 批量页可以选导出分辨率了：预览弹层的「屏幕尺寸」接上之后，批量导出的图片按它出，预览画面比例同步变化；默认沿用「我的偏好」，与聊天页是同一个值。此前批量恒定 1125×2436。
- 批量页的「图片大小」与「字体大小」按组生效：在批量页的预览弹层里改的是当前选中那一组，同批其他组不受影响。新建的组仍按「我的偏好」初始化；这里改的值不会回流成偏好，想让它成为长期默认请到聊天页改一次。
- 批量页也有昵称记忆了：批量自己的「聊天名称」输入框下面多了「最近用过」筹码行，导入聊天记录时解析出的角色名也会一并记进历史，从批量页导入完再回聊天页，名字不用重打。

### 修复

- 取景框里的手机不再上下各空一块：预览容器的显示高度以前拿的是**原始屏幕比例**，而手机本身已经按「画布去掉两侧留白」的比例渲染（更高），两者对不上——于是取景框里看上去像四周都留了白，底部还会被切掉一点。现在容器高度与手机用同一份尺寸换算，手机真的铺满上下、只在左右让出设定的留白。
- 在聊天记录文本末尾补的那一行不再凭空消失：认不出格式的行（没写「姓名：」）以前被整行丢掉，导入回执却照旧显示「导入成功」，用户看到的就是「明明加了一条，怎么没有」。现在这种行会按**上一句的说话人**收进对话，原文一个字都不改；只有「姓名：」后面空着的半行会被跳过，并且回执会写清是第几行。
- 只 @ 了某个人、别的一个字都没有的消息不会再整条丢掉：过滤 @ 称呼时如果会把这行清空，就保留原文。@ 后面还有正文时仍按老规矩把称呼去掉。

## [0.1.0] - 2026-09-11

### 新增

- 独立工具工作页：左侧导航、主编辑区和固定预览区，延续浅绿色界面。
- 批量聊天图：AI 模板 Prompt、多种文本分组识别、分组编辑和一键 ZIP 导出。
- 全局导出日志：查看本机各工具的导出结果、筛选记录和打开对应工具；日志不作为图片备份。
- 账户内支付宝充值：次数包、日卡、月卡、年卡，以及固定 30 天首发优惠说明。
- 网站更新公告：首次提示、关闭后记忆、顶部入口随时重看。

### 改进

- 统一选择器、输入框和确认弹窗，减少空白占位，适配窄屏工作区。
- 批量导出自动下载 ZIP，失败项可重试，已完成图片重复下载不再扣次。
- 更清楚的邮箱校验、注册、登录和密码找回提示。
- 条件化分享奖励提示与埋点；示例姓名迁移仅作用于未修改的默认示例。

### 修复

- AI 输出的 Markdown 标题与不同数量分隔线可正确拆分独立聊天。
- 短对话长截图保留完整手机高度，长对话自动延伸。
- 登录错误不再残留到注册和找回密码界面。
- 导出与支付绑定原账号，切换账号时停止后续扣次，旧请求不会覆盖新账号额度。
- 已到账订单不再占用下一次购买的请求编号；不确定请求仍保留用于安全重试。
