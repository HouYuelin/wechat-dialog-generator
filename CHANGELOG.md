# 更新日志

## 未发布

### 新增

- 定时播放：预览区新增播放条，聊天内容按节奏逐条出现而不是一次全部展示，可选择快 / 正常 / 慢三档速度，方便直接录屏；播放免费，不消耗导出额度。
- 发送消息音效：自己发出消息时也有一声提示音，音色和「收到」明显不同（微信式「咻」），可在播放条和视频导出弹窗里分别开关，默认关闭。
- 视频导出：新增「生成视频」入口，把逐条播放的过程录成视频并下载。导出弹窗内可选画面尺寸（9:16 竖屏 / 1125×2436 满屏 / 跟随屏幕尺寸）、播放节奏、收到与发送两种音效的独立开关，以及提示音音源（内置合成音或上传自己的音频）。
- 收到消息提示音：视频导出时按消息节奏播放微信风格提示音，内置音由 Web Audio 实时合成，不引入第三方音频文件。
- 屏幕尺寸（导出分辨率）：预览弹层内可选 1080×1920 / 1080×2340 / 1125×2436 / 1170×2532 / 1290×2796 / 1440×3120 等常见机型分辨率，也可自定义宽高。导出的图片与视频帧按它输出，预览画面比例同步变化。
- 图片大小：预览弹层内新增一段，可选 小 300 / 标准 420 / 大 540 / 超大 700，也可自定义最长边（160–700px）。此前图片消息固定按最大边 420px 渲染，长图和主图会显得偏小。
- 素材库：上传过的头像与表情图片都会留在这台浏览器里（IndexedDB 的 `media-assets`），下次在任意对话中直接点选复用，也可以重新上传。弹窗内可改名、删除，并按「头像 / 表情图片」两类给出数量与占用体积。
- 批量上传：素材库弹窗支持一次选多张，或把一堆图直接拖进来；「角色头像」面板新增「批量上传头像」，按选择顺序为角色逐个配图，多出的只入库不影响已有角色。

### 改进

- 素材库每类最多保留 80 张，超出时淘汰最久没用过的；同一张图重复上传只会留一条。删除素材不会影响已经用上的头像与图片（它们是抄进项目里的副本）。
- 上传入口统一到一条链路：点头像卡片上传、从素材库选、给某条图片消息换图，走的都是「读图 → 超过最长边 1280px 才压缩 → 入库」，动图与矢量图原样保留。素材库不可用（隐私模式等）时上传照常，只是留不到下次。
- 图片消息气泡点击改为打开素材库（含现传的新图），素材库不可用时退回原来的文件选择。
- 视频结尾那一帧的停留时间从 1.6 秒延长到 3 秒，录屏导出都有更充裕的收尾时间（预览播放与导出一致，改的是同一个常量 `playbackTailMs`）。
- 重新导入聊天记录不再丢失已上传的头像：头像与「自己」身份按**名字**复用（每次解析用户 id 都会重排，名字才稳定），导入成功的提示会说明沿用了几个头像。批量导出改用同一份复用规则。
- 内置提示音换成微信风格：接收音改为带钟琴式泛音的「叮咚」（基音 + 2/3/4.76 倍泛音，越高的泛音衰减越快），发送音改为带通扫频气流声叠快速下滑音的「咻」。仍然是现场合成，不打包任何第三方音频文件。
- 游客每日免费导出额度从 10 次调整为 100000 次，额度常量收敛为 `GUEST_DAILY_LIMIT`，界面、分享卡片与 SEO 描述统一引用该值。
- 预览区新增「专注模式」，可隐藏编辑区放大预览，便于录屏。
- 预览区新增「聊天窗口大小」调节：自适应 / 小 320px / 标准 375px / 大 430px / 超大 500px，也可手动输入 200–720px。它只影响页面上的显示大小，成片像素改由「屏幕尺寸」决定。
- 播放与视频导出共用同一套时间线，预览看到的速度即导出后的速度。
- 导出日志与产品埋点补充 `video` 导出类型与 `video_exported` 事件。
- 播放进行中会禁用图片导出，避免录屏中途截到不完整的对话。

## [0.1.0] - 2026-09-11

### 新增

- 独立工具工作页：左侧导航、主编辑区和固定预览区，延续浅绿色界面。
- 批量聊天图：AI 模板 Prompt、多种文本分组识别、分组编辑和一键 ZIP 导出。
- 全局导出日志：查看本机各工具的导出结果、筛选记录和打开对应工具；日志不作为图片备份。
- 账户内支付宝充值：次数包、日卡、月卡、年卡，以及固定 30 天首发优惠说明。
- 网站更新公告：首次提示、关闭后记忆、顶部入口随时重看。

### 改进

- 统一选择器、输入框和确认弹窗，减少空白占位，适配窄屏工作区。
- 批量导出自动下载 ZIP，失败项可重试，已完成图片重复下载不再扣次。
- 更清楚的邮箱校验、注册、登录和密码找回提示。
- 条件化分享奖励提示与埋点；示例姓名迁移仅作用于未修改的默认示例。

### 修复

- AI 输出的 Markdown 标题与不同数量分隔线可正确拆分独立聊天。
- 短对话长截图保留完整手机高度，长对话自动延伸。
- 登录错误不再残留到注册和找回密码界面。
- 导出与支付绑定原账号，切换账号时停止后续扣次，旧请求不会覆盖新账号额度。
- 已到账订单不再占用下一次购买的请求编号；不确定请求仍保留用于安全重试。
